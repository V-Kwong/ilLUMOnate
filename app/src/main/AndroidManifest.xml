<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.emergence.illumonate2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_illuminate"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WhatIsCancer"></activity>
        <activity android:name=".Lifestyle"></activity>
        <activity android:name=".FriendsAndFamily"></activity>
        <activity android:name=".SupportGroup"></activity>
    </application>

</manifest>